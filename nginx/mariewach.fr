# Vhost pour le site vitrine ostéo.
# Installation :
#   sudo cp nginx/mariewach.fr /etc/nginx/sites-available/
#   sudo ln -s /etc/nginx/sites-available/mariewach.fr /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#   sudo certbot --nginx -d mariewach.fr -d www.mariewach.fr   (une fois le DNS créé)

server {
    listen 80;
    listen [::]:80;
    server_name mariewach.fr www.mariewach.fr;

    root /var/www/mariewach.fr;
    index index.html;

    # astro build.format "file" génère /page.html : essayer $uri.html
    location / {
        try_files $uri $uri.html $uri/index.html =404;
    }

    error_page 404 /404.html;

    # Cache long pour les assets fingerprintés par Astro
    location /_astro/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    gzip on;
    gzip_types text/css application/javascript image/svg+xml;
}
